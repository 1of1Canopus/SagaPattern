apiVersion: v1
kind: Service
metadata:
  name: saga-pattern
  namespace: saga-pattern
spec:
  selector:
    app: saga-pattern
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
